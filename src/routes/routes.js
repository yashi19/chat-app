import Login from "../components/Login/Login";
import App from "../components/App/App";

const routes = [
    {
        path: "/",
        component: Login
    },
    {
        path: "/app",
        component: App
    }
];

export default routes;